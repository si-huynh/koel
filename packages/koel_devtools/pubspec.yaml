name: koel_devtools
description: Flutter DevTools extension — observer, time-travel replay, JSONL trace export.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.38.0"

resolution: workspace

dev_dependencies:
  koel_lints:

# Real LangGraph protocol fixtures land here in Epic 5 Story 5.6
# (koel_langgraph — Fixtures + ErrorClassifier + ConformanceRunner green).
# Captured live from a real LangGraph deployment per FR-G1; until then this
# directory is intentionally empty of fixtures.
# Do not hand-author captures here — synthesized fixtures live in ../synthesized/.

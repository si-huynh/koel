## 1.0.0

- First stable release — the CopilotKit runtime adapter (`CopilotRuntimeAgent`),
  native AG-UI/SSE for CopilotKit v2 (≥1.52), built on `koel_http`'s `HttpAgent`.

## 0.0.1

- Initial scaffold.

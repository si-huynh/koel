## 1.0.1

- FIX (packaging): republish with the generated source
  (`conformance_report.freezed.dart`) included in the archive — `.gitignore`
  excluded it from the 1.0.0 upload, leaving the package uncompilable for
  hosted consumers. No API changes.

## 1.0.0

- First stable release — the koel test harness: mock agent, synthesized +
  captured fixtures, fixture loader, tool-handler harness, and the
  `ConformanceRunner` (FR-G4).

## 0.0.1

- Initial scaffold.

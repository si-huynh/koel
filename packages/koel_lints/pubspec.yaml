name: koel_lints
description: Analyzer plugin enforcing koel's mandatory rules (analysis_server_plugin).
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.11.0 <4.0.0"

resolution: workspace

dependencies:
  analysis_server_plugin: ^0.3.15
  analyzer: ^13.0.0

dev_dependencies:
  analyzer_testing: ^0.2.6
  lints: ^6.0.0
  test: ^1.20.2
  test_reflective_loader: ^0.4.0
